# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0,<2.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# Dataset and augmentation
albumentations>=1.3.0

# Model training
segmentation-models-pytorch>=0.3.3
tqdm>=4.65.0

# Configuration and logging
pyyaml>=6.0
tensorboard>=2.13.0

# Data handling
pandas>=2.0.0
openpyxl>=3.1.2

# Visualization
plotly>=5.20.0
seaborn>=0.13.2
matplotlib>=3.10.0      # usado no script de unificação (gráfico de contagem por base)

# Jupyter / Notebook support (necessário para plotly)
nbformat>=4.2.0
ipython>=8.18.0
ipywidgets>=8.1.0

# Napari and image processing
napari[pyqt5]>=0.4.18
tifffile>=2023.1.1

# Utilities
pathlib2>=2.3.7
findpapers==0.6.7

# Literature review pipeline (unificação/dedup de exports Scopus, WoS, PubMed, Embase, IEEE)
rispy>=0.10.0           # parser de arquivos .ris (Scopus, WoS, Embase, IEEE Xplore)
biopython>=1.87         # parser de .nbib/MEDLINE (PubMed)
rapidfuzz>=3.14.0       # fuzzy matching de títulos para deduplicação